{{ 'sections-common.css' | asset_url | stylesheet_tag }}

{% style %}
  .faq-new.section-{{ section.id }} {
    padding-top: {{ section.settings.padding_top | times: 0.65 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.65 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .faq-new.section-{{ section.id }} {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{% endstyle %}

<div id="{{- section.settings.id -}}" class="faq-new section-{{ section.id }}">
  <div class="container">
    <div class="faq-new__box">
      <div class="faq-new__left{% if section.settings.table_show %} faq-new__left--after{% endif %}">
        {%- if section.settings.table_show -%}
          <div class="table-content">
            <h5 class="table-content__title">
              Topics
            </h5>
            <div class="table-content__links">
              {%- if section.settings.first_table_link != blank -%}
                <a href="#{{ section.settings.first_table_link }}" class="table-content__link">
                  <span>
                    {% render 'icon-article-new-arrow' %}
                  </span>
                  {{- section.settings.first_table_text -}}
                </a>
              {%- endif -%}

              {%- if section.settings.second_table_link != blank -%}
                <a href="#{{ section.settings.second_table_link }}" class="table-content__link">
                  <span>
                    {% render 'icon-article-new-arrow' %}
                  </span>
                  {{- section.settings.second_table_text -}}
                </a>
              {%- endif -%}

              {%- if section.settings.third_table_link != blank -%}
                <a href="#{{ section.settings.third_table_link }}" class="table-content__link">
                  <span>
                    {% render 'icon-article-new-arrow' %}
                  </span>
                  {{- section.settings.third_table_text -}}
                </a>
              {%- endif -%}

              {%- if section.settings.fourth_table_link != blank -%}
                <a href="#{{ section.settings.fourth_table_link }}" class="table-content__link">
                  <span>
                    {% render 'icon-article-new-arrow' %}
                  </span>
                  {{- section.settings.fourth_table_text -}}
                </a>
              {%- endif -%}

              {%- if section.settings.fifth_table_link != blank -%}
                <a href="#{{ section.settings.fifth_table_link }}" class="table-content__link">
                  <span>
                    {% render 'icon-article-new-arrow' %}
                  </span>
                  {{- section.settings.fifth_table_text -}}
                </a>
              {%- endif -%}

              {%- if section.settings.sixth_table_link != blank -%}
                <a href="#{{ section.settings.sixth_table_link }}" class="table-content__link">
                  <span>
                    {% render 'icon-article-new-arrow' %}
                  </span>
                  {{- section.settings.sixth_table_text -}}
                </a>
              {%- endif -%}

              {%- if section.settings.seventh_table_link != blank -%}
                <a href="#{{ section.settings.seventh_table_link }}" class="table-content__link">
                  <span>
                    {% render 'icon-article-new-arrow' %}
                  </span>
                  {{- section.settings.seventh_table_text -}}
                </a>
              {%- endif -%}
            </div>
          </div>
          <select
            class="table-content__select"
            id="tableOfContents"
            onchange="goToLink(this)">
            <option value="">Select topic</option>
            {%- if section.settings.first_table_link != blank -%}
              <option value="#{{ section.settings.first_table_link }}">{{ section.settings.first_table_text }}</option>
            {%- endif -%}
            {%- if section.settings.second_table_link != blank -%}
              <option value="#{{ section.settings.second_table_link }}">{{ section.settings.second_table_text }}</option>
            {%- endif -%}
            {%- if section.settings.third_table_link != blank -%}
              <option value="#{{ section.settings.third_table_link }}">{{ section.settings.third_table_text }}</option>
            {%- endif -%}
            {%- if section.settings.fourth_table_link != blank -%}
              <option value="#{{ section.settings.fourth_table_link }}">{{ section.settings.fourth_table_text }}</option>
            {%- endif -%}
            {%- if section.settings.fifth_table_link != blank -%}
              <option value="#{{ section.settings.fifth_table_link }}">{{ section.settings.fifth_table_text }}</option>
            {%- endif -%}
            {%- if section.settings.sixth_table_link != blank -%}
              <option value="#{{ section.settings.sixth_table_link }}">{{ section.settings.sixth_table_text }}</option>
            {%- endif -%}
            {%- if section.settings.seventh_table_link != blank -%}
              <option value="#{{ section.settings.seventh_table_link }}">{{ section.settings.seventh_table_text }}</option>
            {%- endif -%}
          </select>
        {%- endif -%}
      </div>
      <div class="faq-new__right">
        <h2 class="faq-new__right--title">
          {{- section.settings.heading -}}
        </h2>
        <div class="faq-new__right--items">
          {% for block in section.blocks %}
            <div class="faq-new__right--item">
              <button class="accordion" type="button" aria-expanded="false">
                <div class="faq-new__right--item-box">
                  {% if forloop.index > 9 %}
                    <span>{{ forloop.index }}</span>
                  {% else %}
                    <span>0{{ forloop.index }}</span>
                  {% endif %}

                  <h5 data-text="{{ block.settings.heading | escape }}">
                    <span>{{- block.settings.heading -}}</span>
                  </h5>
                </div>
                <span class="faq-new__right--item-toggle" aria-hidden="true"></span>
              </button>
              <div class="panel-custom">
                <p class="panel-inner faq-new__right--item-text">
                  {{ block.settings.subheading }}
                </p>
              </div>
            </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</div>

{%- comment -%}
  The Topics box is position:sticky, but sticky can only stick inside its own
  parent - and each FAQ topic is a separate section. So it unpinned after the
  first topic group. This pins it across every .faq-new section on the page.
  Desktop only; mobile uses the select dropdown instead.
{%- endcomment -%}
{%- if section.settings.table_show -%}
<script>
(function () {
  var TOP = 94, MIN_W = 750;
  function init() {
    var secs = [].slice.call(document.querySelectorAll('.faq-new'));
    if (secs.length < 2) return;
    var col = document.querySelector('.section-{{ section.id }} .faq-new__left');
    if (!col) return;

    var ph = document.createElement('div');
    ph.setAttribute('aria-hidden', 'true');
    ph.style.display = 'none';
    col.parentNode.insertBefore(ph, col);

    var pinned = false, natural = null;

    function measure() {
      var r = col.getBoundingClientRect();
      natural = { left: r.left + window.scrollX, width: r.width, height: r.height,
                  top: r.top + window.scrollY };
    }
    function unpin() {
      if (!pinned) return;
      col.style.position = ''; col.style.top = ''; col.style.left = '';
      col.style.width = ''; ph.style.display = 'none';
      pinned = false;
    }
    function pinAt(topPx) {
      if (!pinned) {
        ph.style.display = 'block';
        ph.style.width = natural.width + 'px';
        ph.style.height = natural.height + 'px';
        ph.style.flex = '0 0 ' + natural.width + 'px';
        col.style.position = 'fixed';
        col.style.width = natural.width + 'px';
        col.style.left = (natural.left - window.scrollX) + 'px';
        pinned = true;
      }
      col.style.top = topPx + 'px';
    }
    function update() {
      if (window.innerWidth < MIN_W || getComputedStyle(col).display === 'none') { unpin(); return; }
      if (!pinned) measure();
      var last = secs[secs.length - 1];
      var regionBottom = last.getBoundingClientRect().bottom + window.scrollY;
      var h = pinned ? col.offsetHeight : natural.height;
      var y = window.scrollY;

      if (y + TOP < natural.top) { unpin(); return; }
      var overshoot = (y + TOP + h) - regionBottom;
      if (overshoot > 0) {
        if (y + TOP - overshoot < natural.top) { unpin(); return; }
        pinAt(TOP - overshoot);
      } else {
        pinAt(TOP);
      }
    }
    function onResize() { unpin(); measure(); update(); }

    measure();
    update();
    window.addEventListener('scroll', update, { passive: true });
    window.addEventListener('resize', onResize);
    window.addEventListener('load', onResize);
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init);
  else init();
})();
</script>
{%- endif -%}

<script>
  $('.section-{{ section.id }} .accordion').click(function (e) {
    e.preventDefault();
    let $this = $(this);
    if ($this.next().hasClass('show')) {
      $this.next().removeClass('show');
      $this.removeClass('active');
      $this.attr('aria-expanded', 'false');
    } else {
      $this.next().addClass('show');
      $this.addClass('active');
      $this.attr('aria-expanded', 'true');
    }
  });
  
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
  anchor.addEventListener('click', function(e) {
    e.preventDefault();
    const target = document.querySelector(this.getAttribute('href'));
    if (target) {
      target.scrollIntoView({
        behavior: 'smooth',
        block: 'center'
      });
    }
  });
  });
  
  function goToLink(select) {
        const value = select.value;
        if (value.startsWith("#")) {
            const target = document.querySelector(value);
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth',
                    block: 'center'
                });
            }
        }
    }
</script>

{% schema %}
  {
    "name": "FAQ new",
    "settings": [
      {
        "type": "text",
        "id": "id",
        "label": "Section id",
        "info": "The ID must be unique for each partition"
      },
      {
        "type": "text",
        "id": "heading",
        "label": "Heading",
        "default": "Heading"
      },
      {
        "type": "header",
        "content": "Table of contents"
      },
      {
        "type": "checkbox",
        "id": "table_show",
        "label": "Show table of contents",
        "info": "There can only be one per page",
        "default": false
      }, {
        "type": "header",
        "content": "First table"
      }, {
        "type": "text",
        "id": "first_table_text",
        "label": "Table text"
      }, {
        "type": "text",
        "id": "first_table_link",
        "label": "Table link",
        "info": "Bring the section id here"
      }, {
        "type": "header",
        "content": "Second table"
      }, {
        "type": "text",
        "id": "second_table_text",
        "label": "Table text"
      }, {
        "type": "text",
        "id": "second_table_link",
        "label": "Table link",
        "info": "Bring the section id here"
      }, {
        "type": "header",
        "content": "Third table"
      }, {
        "type": "text",
        "id": "third_table_text",
        "label": "Table text"
      }, {
        "type": "text",
        "id": "third_table_link",
        "label": "Table link",
        "info": "Bring the section id here"
      }, {
        "type": "header",
        "content": "Fourth table"
      }, {
        "type": "text",
        "id": "fourth_table_text",
        "label": "Table text"
      }, {
        "type": "text",
        "id": "fourth_table_link",
        "label": "Table link",
        "info": "Bring the section id here"
      }, {
        "type": "header",
        "content": "Fifth table"
      }, {
        "type": "text",
        "id": "fifth_table_text",
        "label": "Table text"
      }, {
        "type": "text",
        "id": "fifth_table_link",
        "label": "Table link",
        "info": "Bring the section id here"
      }, {
        "type": "header",
        "content": "Sixth table"
      }, {
        "type": "text",
        "id": "sixth_table_text",
        "label": "Table text"
      }, {
        "type": "text",
        "id": "sixth_table_link",
        "label": "Table link",
        "info": "Bring the section id here"
      }, {
        "type": "header",
        "content": "Seventh table"
      }, {
        "type": "text",
        "id": "seventh_table_text",
        "label": "Table text"
      }, {
        "type": "text",
        "id": "seventh_table_link",
        "label": "Table link",
        "info": "Bring the section id here"
      }, {
        "type": "header",
        "content": "Section padding"
      }, {
        "type": "range",
        "id": "padding_top",
        "min": 0,
        "max": 200,
        "step": 2,
        "unit": "px",
        "label": "Top padding",
        "default": 24
      }, {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 200,
        "step": 2,
        "unit": "px",
        "label": "Bottom padding",
        "default": 24
      }
    ],
    "blocks": [
      {
        "type": "faq",
        "name": "Items",
        "settings": [
          {
            "type": "text",
            "id": "heading",
            "label": "Heading",
            "default": "Our Values"
          }, {
            "type": "textarea",
            "id": "subheading",
            "label": "Subheading",
            "default": "We like to keep it clean and transparent."
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "FAQ new"
      }
    ]
  }
{% endschema %}