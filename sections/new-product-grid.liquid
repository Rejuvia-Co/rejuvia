<div class="new-product-grid container">
    {%- comment -%} Pull from the collection's Title/Description by default; theme settings override per collection. {%- endcomment -%}
    {%- assign intro_heading = section.settings.seo_heading | default: collection.title -%}
    {%- assign intro_text = section.settings.seo_text -%}
    {%- if intro_text == blank -%}{%- assign intro_text = collection.description -%}{%- endif -%}
    {% if intro_heading != blank or intro_text != blank %}
        <div class="collection-intro">
            {% if intro_heading != blank %}
                <h1 class="collection-intro__title">{{ intro_heading }}</h1>
            {% endif %}
            {% if intro_text != blank %}
                <div class="collection-intro__text">{{ intro_text }}</div>
            {% endif %}
        </div>
    {% endif %}
    {% for block in section.blocks %}
        {% if block.type == 'collection-header' %}
            {% assign heading_tag = block.settings.heading %}
            {% assign block_products = '' %}
            {% assign index = 0 %}

            {% for product in collection.products %}
                {% if product.tags contains heading_tag %}
                    {% assign index = index | plus: 1 %}
                    {% capture block_products %}
                        {{ block_products }}
                        {% render 'new-product-grid', product: product %}

                    {% endcapture %}
                {% endif %}
            {% endfor %}

            {% if block_products != blank %}
                <div class="eg-marginY">
                    <div class="product-recommendations">
                        {{ block_products }}
                    </div>
                </div>
            {% endif %}
        {% endif %}
    {% endfor %}
</div>

{% schema %}
{
  "name": "Collection products grid",
  "tag": "section",
  "class": "custom-section",
  "settings": [
    {
      "type": "header",
      "content": "Page intro (SEO)",
      "info": "By default the H1 uses the collection Title and the paragraph uses the collection Description (edited per collection in Products → Collections). Fill these in only to override for the collection(s) using this template."
    },
    {
      "type": "text",
      "id": "seo_heading",
      "label": "H1 heading (override)"
    },
    {
      "type": "richtext",
      "id": "seo_text",
      "label": "Intro paragraph (override)"
    }
  ],
  "blocks": [
    {
      "type": "collection-header",
      "name": "Collection heading",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Tag to Match Products (Heading Tag)"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Display Title"
        }
      ]
    },
    {
      "type": "content_card",
      "name": "Content Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Card image"
        },
        {
          "type": "text",
          "id": "text_over_image",
          "label": "Text over image"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Card content"
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Button text"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Card link"
        },
        {
          "type": "number",
          "id": "insert_after",
          "label": "Insert after product #",
          "default": 2
        },
        {
          "type": "text",
          "id": "group",
          "label": "Tag to Match Group",
          "default": "functional"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Collection products grid"
    }
  ],
  "templates": [
    "collection"
  ]
}
{% endschema %}
